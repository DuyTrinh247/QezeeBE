# Ưu tiên dùng DATABASE_URL nếu có (Neon, Supabase, Railway, Render, ...)
# Ví dụ: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
# DATABASE_URL=

# Hoặc cấu hình theo biến PG*
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=postgres

# Thiết lập SSL cho môi trường cloud (true để bỏ verify cert)
PGSSL=false

# Cổng ứng dụng Express
PORT=3001
